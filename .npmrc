# 本仓库是公开仓库，依赖必须从公网 npm 解析：
# 1. 内网 Artifactory 的 tarball URL 会被写进 pnpm-lock.yaml，GitHub Actions runner 拉不到，CI 必挂
# 2. public 仓库的 lockfile 不应泄露内网地址
# 覆盖 ~/.npmrc 里可能存在的内网 registry 配置。
registry=https://registry.npmjs.org/
